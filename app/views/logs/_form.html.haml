- if @log.errors.any?
  #error_explanation
    %h2= "#{pluralize(@log.errors.count, "error")} prohibited this log from being saved:"
    %ul
      - @log.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :notes
  = f.text_area :notes
.field
  = f.label :log_date
  = f.date_select :log_date
.field
  = f.label :time_minutes
  = f.number_field :time_minutes
.actions
  = f.submit 'Save'
